/*angular.module('routing').factory('contributionsService',contributionsService);

function contributionsService ($http, $q) {
  var SERVER_URL = 'https://hackernewsasw2016.herokuapp.com/contributions/';

  return {

    getContributions: function () {
      /*var userId = localStorage.getItem('wordistId');
       var token = {
       'headers': {
       'authorization': 'Bearer ' + localStorage.getItem('wordistToken')
       }
       }
       var q = $q.defer();
       $http.get(SERVER_URL + userId + '/words', token).then(
       function (words) {
       q.resolve(words);
       }, function (err) {
       q.reject(err);
       }
       );
       return q.promise;
       }//COMENTARTODOESTo
      var q = $q.defer();
      $http.get(SERVER_URL).then(
          function (contributions) {
            q.resolve(contributions)
          }, function (error) {
            q.reject(error)
          }
      );
      console.log(q);
    }
  }
}
*/
